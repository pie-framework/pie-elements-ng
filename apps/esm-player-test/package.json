{
  "name": "esm-player-test",
  "version": "1.0.0",
  "private": true,
  "description": "Test app for validating published ESM PIE elements with the ESM player",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:evals": "playwright test",
    "test:evals:ui": "playwright test --ui",
    "test:evals:headed": "playwright test --headed"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "vite": "^6.0.0"
  }
}
