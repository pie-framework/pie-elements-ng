---
"@pie-lib/math-input-svelte": patch
---

`StaticMath` renders its LaTeX through the shared MathJax renderer, `MathField` uses Svelte 5 runes, and the type declarations in `dist` resolve on their own. The package remains a placeholder with no math editor (PIE-1075).
