---
"@pie-lib/styling-svelte": patch
---

Add the `@pie-lib/render-ui` colour tokens this package lacked: `disabledText`, `surface`, the table, keypad and button tokens, `buttonFocusOutline` and `SHAPES_FILL_COLOR`, with render-ui's values and fallbacks (PIE-1075).
