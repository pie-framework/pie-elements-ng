---
'@pie-element/graphing-solution-set': patch
---

The author element merges the host's configuration over its defaults, as the element contract requires, so a configuration without `language` no longer throws.
